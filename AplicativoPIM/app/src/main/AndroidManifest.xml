<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões existentes -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Nova permissão para notificações -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicativoPIM"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".model.Carrinho.Carrinho"
            android:exported="false">

        </activity>

        <activity
            android:name=".model.Checkout"
            android:exported="false">

        </activity>

        <activity
            android:name=".model.DetalheProduto.DetalheProduto"
            android:exported="false">
        </activity>

        <activity
            android:name=".model.DetalhesPedidos"
            android:exported="false">
        </activity>

        <activity
            android:name=".model.ListaProdutos.ListaMostrar"
            android:exported="false">

        </activity>

        <activity
            android:name=".model.Login.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>

        <activity
            android:name=".model.MainActivity"
            android:exported="false">

        </activity>

        <activity
            android:name=".model.Pedidos.MeusPedidos"
            android:exported="false" >

        </activity>

        <activity
            android:name=".model.Perfil"
            android:exported="false" >

        </activity>

        <activity
            android:name=".model.RegistrationActivity"
            android:exported="false" >

        </activity>

        <activity
            android:name=".model.FaleConosco"
            android:exported="false" >

        </activity>
    </application>

</manifest>